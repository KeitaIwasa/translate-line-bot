## グループ言語設定を5件に制限する対応

- [x] 設定値追加: `MAX_GROUP_LANGUAGES` 環境変数を `src/config.py` で読み込みデフォルト5に設定し、`MessageHandler` や `PostbackHandler` へ渡す経路を用意する。
- [x] 言語登録フロー抑止: `_attempt_language_enrollment` で6件以上の対応言語を検出した場合は保存・確認テンプレートを出さず、5件以内で再指定するよう通知するガードを追加する。
- [x] コマンド経由の追加制御: `_handle_language_settings` で現在設定＋追加予定の言語数が上限を超える場合は何も更新せず警告応答を返す処理を入れる（add/add_and_remove双方）。
- [x] Postback防御: `PostbackHandler` 側でも受信した言語リストが上限超過の場合に保存を拒否し、再指定を促す応答を返す。
- [x] 文言整理: 上限超過時に返す多言語メッセージを共通化し、指示言語が分かる場合はそれに合わせて提示する。
- [x] テスト追加: 言語登録フロー・コマンド処理で上限超過を拒否する単体テストを追加し、既存テストのコンストラクタ引数変更にも対応させる。
- [x] ドキュメント反映: 新しい上限仕様（6件以上は全拒否して再入力要求）と `MAX_GROUP_LANGUAGES` の説明を `AGENTS.md` / README など関連箇所に追記。
- [x] 動作確認: 全テスト `pytest` を通し、ステージングへデプロイして挙動を確認する。
